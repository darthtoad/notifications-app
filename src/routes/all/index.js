import { h } from 'preact';
import style from './style';

const All = () => (
	<div class={style.home}>
		<h1>All Notifications</h1>
		<p>This is where scheduled notifications will go.</p>
	</div>
);

export default All;